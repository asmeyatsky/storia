[package]
name = "storia-events"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

# Layer rule (Rules §2): this crate is domain-shaped. NO I/O dependencies.
# cargo-deny enforces (deny.toml: storia-events may not wrap sqlx).
[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
uuid = { workspace = true }
chrono = { workspace = true }
thiserror = { workspace = true }
