[package]
name = "storia-connectors-mews"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

# Layer (Rules §2): infrastructure — implements a domain port via JSON-RPC into the
# Python application layer (or directly via shared schema).
# May depend on storia-events; reverse is forbidden by cargo-deny.

[dependencies]
storia-events = { path = "../storia-events", version = "0.1" }
tokio = { workspace = true }
reqwest = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
sqlx = { workspace = true }
anyhow = { workspace = true }
thiserror = { workspace = true }
uuid = { workspace = true }
chrono = { workspace = true }
tracing = { workspace = true }
